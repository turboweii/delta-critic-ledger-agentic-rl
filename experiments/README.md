# Experiments

This directory stores experiment manifests and run notes.

Suggested ablations:

- `terminal_only`: binary terminal reward only.
- `delta_only`: terminal reward plus Delta-Critic.
- `ledger_only`: terminal reward plus Evidence Ledger bonus.
- `delta_ledger`: combined reward.

The current standalone demo is intentionally small and deterministic.

